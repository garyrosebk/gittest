spring:
  profiles: 
    active: zone1
  application:
    name: client-service
  cloud:
    config:
      fail-fast: true
      discovery:
        service-id: config-server
#       uri: http://localhost:8888
#       token: client