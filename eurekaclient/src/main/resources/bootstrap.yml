spring:
  profiles: 
    active: default
  application:
    name: client-service
  cloud:
    config:
#      discovery:
#        service-id: config-server
       uri: http://localhost:8888
       token: aaa
